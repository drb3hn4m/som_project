name: Deploy SOM'

on:
  push:
    branches:
    - 'master'
env:
  PROJECT_ID: ${{ secrets.GKE_PROJECT }}

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:

    - name: code checkout
      uses: actions/checkout@v2

    - name: install the gcloud cli
      uses: google-github-actions/setup-gcloud@v0
      with:
        project_id: ${{ secrets.GKE_PROJECT }}
        service_account_key: ${{ secrets.GKE_SA_KEY }}
        export_default_credentials: true
    - uses: 'google-github-actions/get-gke-credentials@v2'
      with:
        cluster_name: 'som-cluster'
        location: 'us-central1-a'
  
    - name: build and push the docker image

      run: |
        gcloud auth configure-docker us-central1-docker.pkg.dev
        docker build -t us-central1-docker.pkg.dev/$PROJECT_ID/somapp/somimage:latest .
        docker push us-central1-docker.pkg.dev/$PROJECT_ID/somapp/somimage:latest

    - name: deploy to gke
      run: |
        kubectl apply -f som-k8s-gcp/resources.yaml